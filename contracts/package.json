{
  "name": "@midnight-sealed-bid-marketplace/contract",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compact": "compact compile src/marketplace.compact src/managed/marketplace",
    "build": "echo \"Skipping compact compilation in CI\" && exit 0",
    "test": "echo \"Tests not yet implemented\" && exit 0"
  },
  "devDependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0"
  }
}
